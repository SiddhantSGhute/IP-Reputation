[MongoDB]
connection = mongodb connection string

[Abuse]
key = abuseipdb public key

[Virus]
key = virustotal public key

[Mail]
user = login user
pass = app password /Avoid direct password
smtp = smtp server name
default = default email id to receve every mail